# CLAUDE.md — Contexte projet Awtodo

Ce fichier est l'**index** du contexte Awtodo pour Claude Code. Découpé en plusieurs fichiers (voir "Documentation détaillée" ci-dessous) le 06/08/2026 suite à un diagnostic `codeburn optimize` montrant des relectures répétées coûteuses sur un fichier unique de 800+ lignes. Ce fichier reste la **source de vérité sur les décisions déjà tranchées** — ne pas les remettre en question sans validation explicite.

## 📚 Documentation détaillée (fichiers séparés)

Ce fichier est un **index volontairement court**. Le détail vit dans des fichiers séparés, à lire seulement quand la tâche en cours le justifie — pas systématiquement à chaque session (voir aussi la section "Skills installés" pour `codeburn`, qui permet de vérifier si ce découpage réduit bien les relectures inutiles) :

- **`docs/modeles-et-api.md`** — modèles de données, tous les endpoints (tâches, incidents, projets, versions, clôture), mécanisme d'identification temporaire, scoping, permissions, hub projet, corrections UI historiques.
- **`docs/organisation-et-comptes.md`** — hiérarchie plateforme/organisation/projet/groupe, écrans d'administration, comptes et invitations.
- **`docs/charte-graphique.md`** — couleurs, typographie, formes, librairies de composants front, patterns UI.

**Règle d'usage :** dans un prompt, citer explicitement quel(s) fichier(s) lire selon la tâche (ex. "lis `docs/organisation-et-comptes.md`"), plutôt que de systématiquement demander de tout lire — c'est tout l'intérêt du découpage.

## Vue d'ensemble du projet

**Awtodo** est une webapp interne d'entreprise de gestion de projets (collaboratifs ou individuels), incluant gestion de tâches priorisées, gestion d'incidents, planification, et statistiques.

**Positionnement produit important :** Awtodo n'est PAS un clone de Trello/Jira/Asana. L'objectif est un outil d'organisation et de gestion personnalisable, taillé pour des besoins internes spécifiques, pas un outil générique pour chefs de projet certifiés. En cas de doute sur une fonctionnalité, privilégier la simplicité et la flexibilité (champs/statuts configurables) plutôt que la complétude façon outil pro de gestion de projet.

**Contexte d'usage :** application interne d'entreprise, pas de grand public. Authentification via SSO Microsoft obligatoire. L'API doit rester totalement ouverte car connectée à un outil de ticketing externe et à des automatisations (Power Automate notamment).

## Stack technique

- **Backend :** Django + Django REST Framework (DRF)
- **Frontend :** React (choisi pour l'écosystème de composants Kanban/Gantt/drag-and-drop et la maturité de `@azure/msal-react` pour le SSO)
- **Base de données :** PostgreSQL (via AWS RDS en prod) — ⚠️ direction actée, implémentation NON autorisée pour l'instant, voir encadré ci-dessous
- **Tâches async / événementiel :** Celery + Redis (broker Redis via ElastiCache en prod)
- **Auth :** SSO Microsoft via Azure AD / Entra ID, protocole OIDC (pas SAML) — cible finale, toujours différée (tenant Azure AD réel requis)
  - Backend : `mozilla-django-oidc` ou `django-allauth` (provider Microsoft) — à trancher lors de l'implémentation du SSO
  - API : JWT via `djangorestframework-simplejwt` — **câblé (session du 2026-08-06)**, mais pour une authentification par mot de passe indépendante du SSO (login + activation de compte via invitation, voir `docs/organisation-et-comptes.md` > "Authentification par mot de passe"), pas encore pour des service accounts/clés API machine-to-machine dédiés (ticketing, Power Automate) — ce découpage-là reste à faire
  - `django-allauth` reste une dépendance déclarée mais non câblée, réservée au futur chantier SSO (provider Microsoft), pas utilisée pour le login par mot de passe actuel
  - **Exception actée (session du 2026-09-03) à « la connexion est la porte d'entrée obligatoire du site » (`App.tsx`)** : les pages de *documentation de projet publiées* sont lisibles sans compte, via une URL `/docs/<token>` (token non devinable, révocable, porté par `DocSpace.public_token` ; endpoint `GET /api/v1/docs/public/<token>/` en `AllowAny`). Seul le contenu **publié** (pages + fiches) est exposé — jamais les données de gestion (tâches, membres, budget, incidents bruts). C'est le **seul** contournement autorisé de ce garde-fou. Voir `docs/modeles-et-api.md` > "Documentation de projet".
- **Déploiement :** AWS (App Runner ou Elastic Beanstalk — pas d'EC2 nu, pas de Kubernetes/EKS à ce stade) — ⚠️ direction actée, implémentation NON autorisée pour l'instant, voir encadré ci-dessous
- **Documentation API :** `drf-spectacular` (OpenAPI/Swagger) — obligatoire pour que les équipes tierces (ticketing, Power Automate) puissent consommer l'API de façon autonome
- **Récurrence calendrier :** `python-dateutil` (`dateutil.rrule`) — moteur RRULE iCal pour le module Planning (session du 2026-09-09), backend uniquement. Le frontend construit/décrit les RRULE à la main (sous-ensemble courant), pas de lib JS ajoutée.

> **⚠️ BDD et déploiement : direction actée mais implémentation bloquée.**
> PostgreSQL et AWS sont les choix de techno retenus, mais le schéma de BDD détaillé et la configuration d'infrastructure AWS n'ont pas encore été travaillés en profondeur. Tant que ce fichier n'a pas été mis à jour avec une section dédiée détaillant ces points :
> - Ne pas créer de fichiers `settings/staging.py` ou `settings/production.py` avec des paramètres AWS/RDS concrets.
> - Ne pas ajouter de dépendances liées au déploiement AWS (`django-storages`, `boto3`, config S3, etc.).
> - Ne pas écrire de Dockerfile, script de déploiement, ou configuration App Runner/Elastic Beanstalk.
> - En local/dev, utiliser SQLite ou un PostgreSQL local minimal (via `dev.py`) suffit largement pour avancer sur le code métier — ça ne nécessite aucune décision d'infra.
> Si une tâche semble nécessiter une de ces actions, s'arrêter et demander confirmation plutôt que d'anticiper.

## Règle d'architecture transverse — AUCUNE suppression physique en base

**Règle absolue, applicable à toutes les entités du système (tâches, incidents, projets, etc.) :** aucune donnée n'est jamais supprimée physiquement de la base de données. Tout objet suit un cycle de vie qui se termine par un statut terminal (`archivée`, `rejetée`, etc.), jamais par un `DELETE`.

Conséquences pour l'implémentation :
- Tous les modèles métier utilisent un pattern de **soft-delete / statut terminal**, pas de suppression Django ORM (`.delete()`) sur ces entités.
- Prévoir un champ de statut (ou équivalent) sur chaque modèle métier permettant de distinguer les objets actifs des objets archivés/rejetés, avec un manager Django par défaut qui filtre les objets actifs (`Task.objects.active()` par exemple), pour ne pas polluer les vues courantes avec l'historique.
- Cette règle a un impact direct sur le modèle de données (voir section Modèles) et doit être respectée dans le design de chaque nouvelle app.

**⚠️ Piège Django à connaître (trouvé et corrigé le 2026-08-04) :** `StatusLifecycleModel` (dans `apps/common/models.py`) définit `objects`/`all_objects`, mais avec plusieurs bases abstraites (`UUIDModel, TimeStampedModel, StatusLifecycleModel`), Django n'hérite le `Meta` que de la **première** base qui en déclare un (`UUIDModel`, qui n'a que `abstract=True`) — le reste, y compris tout réglage de manager sur `StatusLifecycleModel.Meta`, est silencieusement ignoré. **Chaque modèle concret héritant de `StatusLifecycleModel` doit donc déclarer explicitement dans son propre `Meta`** :
```python
class Meta:
    default_manager_name = "all_objects"  # sinon les relations inverses (project.tasks.all()) masquent les objets archivés/rejetés
    base_manager_name = "all_objects"     # sinon le collecteur de suppression en cascade (on_delete=PROTECT) ne voit pas les lignes historiques
```
Déjà fait sur `Task`, `Incident`, `Project`, `ProjectMembership`, `Team`. **À reporter sur tout nouveau modèle basé sur `StatusLifecycleModel`** — sans ça, la règle "aucune suppression physique" ci-dessus est violée en pratique dès qu'on traverse une relation inverse. Régression couverte par `apps/common/tests/test_models.py`.

## Structure du projet

```
watodo/
├── config/                       # settings, urls, wsgi/asgi
│   ├── settings/
│   │   ├── base.py
│   │   ├── dev.py
│   │   ├── staging.py
│   │   └── production.py
│   ├── urls.py
│   ├── wsgi.py
│   └── asgi.py
├── apps/
│   ├── common/                   # mixins, base models, permissions génériques — AUCUNE dépendance vers les autres apps
│   ├── accounts/                 # users, teams, rôles, SSO — dépend de common
│   ├── projects/                 # projets, membres, rôles projet (chef de projet / membre) — dépend de accounts
│   ├── tasks/                    # tâches, statuts, priorité, type, planification — dépend de projects, accounts
│   ├── incidents/                # incidents post-prod — entité séparée des tâches — dépend de projects, accounts
│   ├── budgeting/                # budgets, temps passé (évolution future) — dépend de projects, tasks
│   ├── integrations/             # API keys, webhooks, logs d'appels externes, boîte de réception ticketing — dépend de tout, rien ne dépend d'elle
│   ├── notifications/            # mail, Teams, etc. (à détailler plus tard) — dépend de tout, déclenchée par signals
│   ├── documentation/           # espace de doc utilisateur par projet + page publique — dépend de projects/tasks/incidents
│   └── planning/                 # calendrier perso, planning projet, partage de calendrier (RRULE via python-dateutil) — dépend de projects/tasks/incidents
├── manage.py
└── requirements/
    ├── base.txt
    ├── dev.txt
    └── production.txt
```

### Règle de dépendances entre apps

Une app "basse" dans la hiérarchie ci-dessus ne doit **jamais** importer une app "haute". Si une app a besoin de réagir à un événement d'une autre app sans lien de dépendance direct autorisé, passer par les **Django signals**, pas par un import direct. Si ce besoin se présente souvent entre deux apps précises, c'est un signal que la frontière entre ces apps est peut-être mal placée — en discuter avant de forcer un import.

### Structure interne d'une app métier type

```
apps/<nom_app>/
├── models.py
├── serializers.py       # ou serializers/ si le fichier grossit trop (un fichier par cas d'usage)
├── views.py              # ViewSets DRF
├── permissions.py        # permissions custom spécifiques à l'app
├── filters.py             # django-filter pour l'API (filtrage par statut, assigné, etc.)
├── signals.py             # émis sur create/update, consommés par notifications/integrations
├── services.py            # logique métier — voir règle ci-dessous
├── urls.py
├── admin.py
└── tests/
```

## Règles d'architecture à respecter impérativement

1. **Toute logique métier non triviale va dans `services.py`, jamais dans les serializers ou les vues.** Exemple typique : la fonction qui gère la transition de statut d'une tâche (validation par un chef de projet, passage à `disponible`/`assignée`) doit être une fonction de service testable indépendamment de la couche HTTP, notamment parce que ces transitions ont des règles métier précises (voir section Tâches).

2. **Aucun appel synchrone vers un système externe (ticketing, Power Automate, etc.) dans le flux de requête HTTP.** Toute intégration externe passe par : signal Django → tâche Celery async.

3. **Permissions par objet, pas seulement par type d'objet**, et **rôles multiples possibles sur un même projet**. Un projet collaboratif peut avoir plusieurs chefs de projet simultanément — le rôle est porté par la relation `ProjectMembership` (utilisateur × projet × rôle), jamais par un champ unique `owner` sur `Project`. Permissions DRF custom qui vérifient l'appartenance et le rôle projet par projet.

4. **PK en UUID, pas en auto-increment**, sur tous les modèles exposés via l'API (via une classe abstraite `UUIDModel` dans `apps/common`).

5. **Statuts et champs personnalisables, pas figés en dur** quand c'est pertinent. Le type de tâche (`CORRECTION`/`AJOUT`/`ÉVOLUTION`) et la priorité peuvent rester en `choices` Django au démarrage (v1), mais garder en tête une possible évolution vers un modèle configurable si le besoin de personnalisation par projet se confirme.

6. **Versionning d'API dès le départ** (`/api/v1/...`).

7. **Toute app métier a ses tests dans un dossier `tests/` dédié**.

8. **Lire un fichier avant de le modifier ; grep les appelants avant de modifier une fonction.** Rentabilise l'exploration en amont plutôt que d'éditer à l'aveugle puis corriger par itérations.

## Hors périmètre v1 — à clarifier plus tard

Ces points ont été identifiés mais volontairement non tranchés à ce stade. Ne pas les implémenter sans validation explicite au préalable :

- Système de notifications (email, Teams, autre ?)
- Commentaires sur tâches/incidents
- Historique / audit détaillé des changements de statut (au-delà du simple horodatage de transition)
- ~~Budgétisation fonctionnelle complète~~ — un calculateur simple OPEX/CAPEX par projet est implémenté (session du 2026-08-05, voir `docs/modeles-et-api.md` > "Budgétisation"). Reste hors périmètre : le modèle `BudgetEntry` (temps passé × coût horaire), le rapprochement budget prévisionnel/réel, et tout ce qui dépasse une somme de lignes déclaratives.
- Personnalisation avancée des statuts/types par projet (au-delà des `choices` fixes de la v1)
- **Administration des utilisateurs / organisations (mémo, à trancher plus tard, pas avant le chantier SSO) :** comment un utilisateur rejoint-il Watodo (auto-inscription via le SSO d'entreprise, ou provisioning à part) ? Notion d'"organisation" nécessaire si Watodo doit un jour servir plusieurs entités séparées, ou une seule instance = une seule entreprise suffit ? Comment un groupe est-il créé et qui y invite qui — un chef de projet peut-il inviter directement par email, ou passage obligé par un admin ? Ces questions sont volontairement reportées : pas d'implémentation ni de modélisation avant que le chantier SSO ne soit lancé.

## Ce qui est hors périmètre techniquement (pour l'instant)

- Pas de Kubernetes/EKS — trop lourd en charge opérationnelle pour une équipe novice AWS
- Pas de permissions ultra-granulaires façon Jira — rester sur un modèle de rôles simple (chef de projet / membre)
- Pas de découplage microservices — Django monolithique modulaire (apps découplées) suffit largement à cette échelle

## Skills installés

Les skills communautaires suivants sont installés dans ce projet (.claude/skills/). Ils sont tiers, non officiels Anthropic — à utiliser selon leur déclenchement naturel, sans usage abusif.

- **superpowers** (obra) — impose un workflow structuré : planifier avant de coder, TDD (tests avant implémentation), revue en deux étapes avant de considérer une tâche terminée. À privilégier pour toute nouvelle fonctionnalité non triviale.
- **claude-mem** (thedotmack) — mémoire persistante entre sessions Claude Code : capture le contexte des sessions précédentes et le réinjecte automatiquement. Utile pour garder le fil sur un projet aussi long qu'Awtodo sans tout redécrire à chaque session.
- **impeccable** (pbakaus) — skill de design front, à activer pour tout travail sur les composants React/UI (évite le rendu générique "AI slop" : gradients par défaut, cards imbriquées, typographie Inter par défaut, etc.). Complète le skill `frontend-design` déjà disponible nativement.
- **task-observer** (rebelytics) — meta-skill d'observation : journalise les frictions, corrections répétées et patterns de travail pour proposer des améliorations de skills a posteriori. Ne modifie rien automatiquement, produit un log à revoir.
- **find-skills** (vercel-labs) — skill de découverte : recherche dans l'écosystème ouvert de skills agent (via `npx skills find`) si un besoin exprimé pourrait être couvert par un skill existant plutôt que réinventé à la main. Vérifie la fiabilité des skills trouvés (nombre d'installs, réputation du dépôt) avant de recommander. À utiliser quand une tâche demandée touche un domaine non couvert par les skills déjà installés, avant de proposer d'écrire du code custom.
- **headroom** (chopratejas) — proxy local de compression de contexte (logs, sorties d'outils, fichiers) avant envoi au modèle, réversible. Nécessite un processus à part tournant en continu (`headroom proxy`), pas un skill passif comme les autres — vérifier qu'il tourne avant de compter dessus.
- **graphify** (Graphify-Labs) — construit un graphe de connaissance du code (AST via tree-sitter) pour naviguer le projet sans grep aveugle. Ne se rafraîchit pas automatiquement : relancer `/graphify update` après des modifications significatives, sinon l'index devient périmé.
- **codeburn** (AgentSeal) — tableau de bord local d'usage/coût par session (aucun appel LLM, lecture des transcripts locaux). Sert aussi d'outil de vérification pour les autres skills de cette liste (voir plus bas).
- **ponytail** (DietrichGebert) — pousse vers la solution la plus simple avant d'écrire du code (stdlib/natif avant dépendance, une ligne avant un composant). Gain réel mais mesuré plus modeste que la promesse marketing (~−15% de code en benchmark indépendant, pas −54%) — à garder en tête, pas un miracle.

**Priorité en cas de conflit entre skills :** en cas de recommandation contradictoire (ex. impeccable vs frontend-design sur un choix de design), privilégier les règles explicites de ce fichier CLAUDE.md en premier, puis demander confirmation avant de trancher.

**Installation de nouveaux skills :** si `find-skills` recommande l'installation d'un nouveau skill, demander confirmation avant de l'installer, et ajouter une entrée dans cette section une fois fait.

**Vérification d'usage réel :** un skill installé ne se déclenche que si la tâche en cours le justifie — l'absence de déclenchement sur une session donnée n'est pas forcément un dysfonctionnement (ex. `ui-ux-pro-max` ne s'active pas sur un prompt purement backend, c'est attendu). Pour vérifier qu'un skill sert réellement : `codeburn report` (répartition par outil/activité), grep du chemin du skill (`.claude/skills/<nom>/`) dans les transcripts de session (`~/.claude/projects/`), et pour Ponytail/Graphify des signaux directs (badge `[PONYTAIL]` dans la statusline, fichiers `graph.json`/`GRAPH_REPORT.md` régénérés).

## Roadmap macro (à date du 05/08/2026)

**Session du 2026-09-10 (suite bis) — terminée** : **partage de calendrier — sélecteur vide corrigé + combobox recherchable**. Remontée : sur le déployé, « Choisir une personne » dans le panneau de partage était vide alors qu'un compte avait été invité. Cause : le filtre du `SharePanel` (frontend) ne gardait que `account_type === "interne"` — un invité **externe** (compte créé via une invitation projet) était donc masqué. Corrigé : **le partage n'est lié à aucun groupe ni type de compte** (c'était déjà le cas côté backend — `create_share` n'impose que `owner != grantee`, aucun changement serveur), le sélecteur liste maintenant tous les comptes hors soi-même et hors partages déjà accordés, triés par nom, avec une mention « invitation en attente » pour les comptes `pending`. Au passage, le `<select>` natif est remplacé par un nouveau composant **`Combobox`** recherchable (`frontend/src/components/Combobox.tsx` + `.css`, sur `cmdk` déjà présent — pas de nouvelle dépendance, même base que `CommandPalette`) : déclencheur bordé + panneau `position: absolute` avec champ de recherche et liste défilante, fermeture au clic-extérieur/Échap. Réutilisable ailleurs (sélecteurs de personne des autres dialogues planning/tâches non encore migrés). `frontend` build + `tsc` OK. Aucun test backend touché. Voir `docs/modeles-et-api.md` > "Module Planning" > "Décisions cadrées" (partage sans restriction de groupe) et `docs/charte-graphique.md` > "Combobox recherchable".

**Session du 2026-09-10 (suite) — terminée** : **passe de performance sur les endpoints de liste** — remontée directe (délai perceptible pour charger tâches/incidents/projets, indépendamment du cold-start Render). Mesuré avant : `GET /projects/` 158 requêtes / 12 lignes, `GET /tasks/` 1058 / 96, `GET /incidents/` 290 / 36 — N+1 franc. Cause : `get_*_permissions(user, obj)` déclenche ~10 `SELECT EXISTS` d'appartenance par ligne, plus `get_members` / `get_current_version_id` / `UserSerializer.get_teams` par ligne (et par membre imbriqué). **Correctif** : (1) caches d'appartenances **à portée requête** (`contextvars.ContextVar`) — `prefetched_project_roles(user, project_ids)` (dans `apps/projects/services.py`) et `prefetched_team_memberships(user, team_ids)` (dans `apps/accounts/services.py`), deux `@contextmanager` qui résolvent toutes les appartenances de la page en **une** requête ; `is_project_member` / `is_project_contributor` / `is_project_manager` / `is_active_team_member` lisent ce cache pendant la sérialisation et retombent sur `.exists()` hors contexte (détail / mutations inchangés). Chaque `list()` de viewset enroule la sérialisation dans ces contextes. (2) `select_related` / `Prefetch(to_attr=...)` pour les autres `SerializerMethodField` (membres, version courante, `assignee__team_memberships`) — **uniquement `self.action == "list"`** : les `@action` qui re-sérialisent l'instance après mutation verraient sinon un prefetch figé d'avant la modif. Après : `GET /projects/` 6, `GET /tasks/` 4, `GET /incidents/` 4 — constant quel que soit le volume. Garde-fou : `apps/projects/tests/test_list_performance.py` (budget de requêtes plafonné sur beaucoup de données). 531 tests backend, tous verts. Aucun changement frontend. Voir `docs/modeles-et-api.md` > "Scoping des listes par appartenance" > cache d'appartenances.

**Session du 2026-09-10 — terminée** : quatre points d'évolution/correction. **(1) Tâches auto-assignées sur projet individuel** — `create_task` : sur un projet `individuel`, une tâche sans assigné explicite revient au créateur (= chef de projet, seul membre) plutôt que de passer par "disponible" / "en attente de validation". **(2) "Déjà en production" à la création d'un projet** — nouveau champ `Project.already_in_production` (booléen), coché → pas de date d'échéance (incompatibles, validé dans `create_project`). Pas d'écran d'édition après coup, comme `project_type`. **(3) Rôle "Lecteur" sur un projet** — 3ᵉ valeur de `ProjectMembership.role`, accordé par un chef de projet depuis l'onglet Administration (formulaire "ajouter un membre par email" + sélecteur de rôle par ligne). Un lecteur voit le projet, ses tâches et son cahier des charges / bloc-notes **en lecture seule** ; **pas** d'accès au budget, aux incidents, au planning ni aux statistiques (portée choisie avec l'utilisateur). Séparation `accessible_projects` (inclut les lecteurs — liste de projets, tâches) vs nouveau `contributor_projects` (les exclut — budget/incidents/planning/doc/stats globales) ; nouveau `is_project_contributor` + flag `permissions.can_contribute` que le frontend utilise pour masquer les onglets et affordances. `is_project_member`/`is_project_manager` deviennent au passage `status="active"`-aware (étaient trop permissifs, ne filtraient pas les appartenances `removed`). **(4) Reset mot de passe en "lien direct" pour la phase de test** — nouveau réglage `PASSWORD_RESET_DIRECT_LINK` (`True` en dev + staging, `False` par défaut / cible) : pas d'email (pas de fournisseur transactionnel branché), l'endpoint renvoie `reset_path` et `LoginPage` y redirige directement. Enjeu de sécurité assumé (2 utilisateurs). 528 tests backend, tous verts. Voir `docs/organisation-et-comptes.md` > "Rôle Lecteur" + "Réinitialisation de mot de passe" > mode test, et `docs/modeles-et-api.md` > "Projet" / "Tâches".

**Session du 2026-09-09 (suite) — terminée** : **réinitialisation de mot de passe** — comblait un "hors périmètre explicite" noté à la session du 2026-08-06 (auth par mot de passe), remontée directe d'un utilisateur bloqué sur son identifiant réel. Nouveau modèle `PasswordResetRequest` (`apps/accounts`, même schéma qu'`Invitation` : token UUID public + statut `pending`/`used`/`expired`), lien valide **1 heure** (`PASSWORD_RESET_TOKEN_LIFETIME`, calculé à la volée depuis `created_at`, pas balayé périodiquement — délibérément plus court qu'une invitation, qui n'expire pas dans le temps : un lien de reset donne accès à un compte déjà actif, risque plus élevé en cas de fuite). Recherche par identifiant **ou** email (insensible à la casse) ; **aucune énumération de comptes** — réponse toujours `200` avec un message générique, qu'un compte corresponde ou non ; un compte `pending` n'est pas éligible (c'est le flux d'invitation qui s'applique). Une seule demande active à la fois (la précédente est marquée `expired` à la nouvelle, même pattern que `resend_invitation`). Trois endpoints en `APIView` plutôt qu'un `ViewSet` routé sur `token` (`request`/`<token>/`/`<token>/confirm/`) — évite la collision qu'un routeur DRF classique aurait créée entre `password-reset/request/` et `password-reset/<token>/` (le lookup générique capture n'importe quel segment, y compris littéralement "request"), réglée avec des chemins `<uuid:token>` explicites. **Frontend** : `LoginPage.tsx` bascule dans un second mode ("Mot de passe oublié ?", même carte) pour demander l'identifiant ; nouvelle `ResetPasswordPage.tsx` (miroir direct d'`InvitationAcceptPage.tsx`, atteinte via `/reset-password/<token>/`, même contournement d'URL publique que les invitations/docs publics). 491 → 513 tests backend, tous verts en isolation (`apps.accounts`). **Point relevé, non corrigé (hors périmètre)** : le throttle anonyme DRF (cache Django, jamais réinitialisé entre tests) peut faire échouer des tests sans rapport quand la suite complète tourne d'une traite (préexistant, les nouveaux tests API en ajoutent juste plus — `cache.clear()` ajouté dans `setUp` de la nouvelle classe de tests API pour ne pas y contribuer davantage, mais le problème plus large reste ouvert). Voir `docs/organisation-et-comptes.md` > "Réinitialisation de mot de passe".

**Session du 2026-09-09 — terminée** : **module Planning / Calendrier** (nouvelle app `apps/planning/`, label `planning` — dépend de `common`/`accounts`/`projects`/`tasks`/`incidents`, rien ne dépend d'elle ; nouvelle dépendance backend `python-dateutil` pour le moteur RRULE). Trois volets : (1) **calendrier personnel** — événements libres avec **récurrence iCal RRULE complète** (`dateutil.rrule`, stockée en chaîne `recurrence_rule`), plus affectation de **créneaux** (`ScheduledBlock`) aux tâches/incidents assignés en **glisser-déposer** ; (2) **planning de projet** (`ProjectPlanningEntry`, `kind` jalon/phase/réunion/autre) — partagé, **visible par tous les membres, éditable par les chefs de projet seuls** (nouveau flag `permissions.can_manage_project_planning`) ; (3) **partage de calendrier** — `CalendarShare` (superposition lecture seule de compte à compte, révocable des deux côtés) **et** `EventParticipant` (invitation à un événement précis, une seule ligne vue par tous, éditée par le seul organisateur ; notification `event_invited` via le nouveau signal `event_participant_invited`). **Décisions cadrées avec l'utilisateur** : édition de récurrence **à la série entière uniquement** (pas d'occurrence isolée, pas d'EXDATE/RECURRENCE-ID) ; **planifier un créneau ne change jamais le statut** de la tâche/incident (découplage, `time_spent` reste manuel) ; **pas de lien public** vers un calendrier (éviterait une 2ᵉ exception au garde-fou d'auth) ; fuseau unique `Europe/Paris`, expansion des occurrences en heure locale (stable de part et d'autre des changements d'heure). **Calendrier frontend fait main** (`frontend/src/features/planning/` : `WeekGrid`/`MonthGrid`, positionnement CSS pur, `@dnd-kit/core` déjà présent) — aucune librairie de calendrier ajoutée, cohérent avec la charte. `expand_occurrences` (pure fonction, `MAX_OCCURRENCES=366`) est l'algo critique, testé à part. API sous `/api/v1/planning/` (`handle_exception` global comme `documentation`). 448 → 491 tests backend, tous verts. Voir `docs/modeles-et-api.md` > "Module Planning / Calendrier" et `docs/charte-graphique.md` > "Onglet Planning + calendrier fait main". **Limite v1 assumée** : le panneau « À planifier » ne liste que les tâches (le sérialiseur `Incident` n'expose pas `assigned_to`), les blocs sur incident restent créables via l'API.

**Session du 2026-09-03 — terminée** : **module Documentation de projet** (nouvelle app `apps/documentation/`, label `documentation` — dépend de `projects`/`tasks`/`incidents`, rien ne dépend d'elle). Chaque projet a un espace de documentation utilisateur rédigé **par le chef de projet uniquement** (nouveau flag `permissions.can_edit_documentation`), consultable en ligne via un **lien public non devinable et révocable** (`/docs/<token>`, exception d'auth actée plus haut — page `PublicDocsPage.tsx` autonome, détectée par match d'URL dans `App.tsx` comme les invitations, suit le thème clair/sombre, `@media print` pour l'export PDF via Ctrl+P). Contenu : **pages Markdown arborescentes limitées à 2 niveaux** (garde en service) + deux types de **fiches structurées** (`DocEntry.kind`) — onglet **Fonctionnalités** et onglet **Résolution d'incidents**. Cycle de vie `brouillon`/`publie`/`archive` sur pages et fiches (aucune suppression physique). Amorçage automatique via une **file « À documenter »** (`PendingDocEntry`) alimentée par deux **signaux Django nouveaux** : `task_completed` (émis par `complete_task`, types `ajout`/`evolution` seulement — `correction` exclu) → file Fonctionnalités ; `incident_resolved` (émis par `resolve_incident`, incident rattaché à un projet uniquement) → file Résolution d'incidents. Le chef de projet transforme une entrée de file en fiche pré-remplie en un clic, ou l'ignore. L'onglet Fonctionnalités a aussi un bouton « Amorcer depuis le cahier des charges » (`seed_features_from_spec`, une fiche brouillon par bloc de la section `exigences_fonctionnelles`). Export `.docx` de toute la doc côté navigateur (lib `docx` déjà présente, `features/docs/exportDocx.ts`). API sous `/api/v1/docs/` : `DocSpaceViewSet` (authentifié, `lookup_field="project_id"`) + `PublicDocsView` (`AllowAny`, n'expose que le publié, 404 si token inconnu/révoqué). 411 → 448 tests backend, tous verts. Voir `docs/modeles-et-api.md` > "Documentation de projet" et `docs/charte-graphique.md` > "Onglet Documentation + page publique".

**Session du 2026-08-11 (suite bis) — terminée** : deux ajouts backend sur les incidents, remontés en testant l'API en conditions réelles via un tunnel `localtunnel` temporaire (`X-Debug-User-Id`, voir "Mécanisme d'identification temporaire"). (1) **Changement de priorité conditionné** : nouveau champ `Incident.assigned_to` (nullable, n'existait pas — nuance la "différence volontaire avec les tâches" déjà actée, mais uniquement pour ce point précis, le reste du cycle de vie `start`/`resolve`/`archive` reste ouvert à tout membre du groupe, inchangé) + `POST .../claim/` (auto-assignation, ouverte à tout membre, réassignation sans restriction) + `POST .../update-priority/` (réservé à l'assigné actuel **ou** à un administrateur du groupe concerné — même définition déjà actée ailleurs, `apps.accounts.services.can_manage_team` : créateur du groupe, admin d'organisation ou de plateforme ; pour un incident sur un projet individuel sans groupe, le chef de projet de ce projet joue ce rôle à la place, décidé à la clarification). (2) **Compte de service pour la création automatique** : nouveau `User.is_service_account`, nouvel utilisateur de seed `api.ticketing` (`seed_demo_users`) — `IncidentViewSet.create` passe désormais `actor=None` pour ce compte, ce qui déclenche réellement le contournement de la contrainte de groupe déjà écrit dans `create_incident` pour la future intégration ticketing (jusque-là un chemin mort, jamais atteignable via HTTP puisque `request.user` n'est jamais littéralement `None`). Les deux évolutions testées de bout en bout via l'URL publique temporaire, pas seulement en tests unitaires (336 → 368 tests, tous verts). Voir `docs/modeles-et-api.md` > "API — endpoints Incidents".

**Session du 2026-08-11 (suite) — terminée** : trois retours directs successifs sur les onglets "accès rapide" de l'accueil v3. (1) Cause racine du grignotage des cartes par l'oblique trouvée : `tabPosition` se calait sur le bord de `bg-left` sans compter que `bg-seam`, au même niveau d'empilement, dépasse encore de 2.6% au-delà — corrigé en se calant sur le bord extérieur réel du calque oblique. (2) Le survol (`translateX(10px)`) réduisait le chevauchement sous la diagonale au point d'exposer le coin carré de la carte (capture d'écran à l'appui) — d'abord retiré, puis réintroduit (`14px`) une fois le chevauchement de repos relevé à `48px` (calcul qui couvre maintenant la pente de la diagonale sur toute la hauteur d'une carte, pas seulement son bord supérieur, en plus du survol — voir commentaire `TAB_OVERLAP_PX` dans `HomePage.tsx`). (3) Panneau gauche élargi à 40% de la largeur (demandé) et onglets élargis à `400px` (demandé, "bien plus large pour mieux occuper l'espace disponible") — diagonale translatée vers la droite en conséquence (`51%/43%`, même pente), contenu du panneau gauche agrandi en cohérence (logo, libellé, titre du post-it, items, chiffres du récap). Écart assumé : `LoginPage` garde sa propre diagonale d'origine, pas demandée à changer, les deux écrans ne correspondent plus exactement. Voir `docs/charte-graphique.md` > "Écran d'accueil — panneau oblique v3" > "Retouches (suite)".

**Session du 2026-08-11 — terminée** : retouches sur l'écran d'accueil v3 (panneau oblique), cinq points sur retour direct après relecture. Onglets "accès rapide" agrandis (largeur `220px`→`280px`, police et icône relevées, padding gauche spécifiquement augmenté pour que le nom respire par rapport au bord tucked sous la diagonale) et enrichis d'un descriptif fixe d'utilité sous chaque nom (`QuickAccessTab` distingue désormais `meta` compteur optionnel et `description` phrase toujours présente), en plus des compteurs déjà là — pas à leur place. Mention de la marque ajoutée dans le panneau gauche (jusque-là totalement absente de cet écran) : le vrai logo `awtodo-logo.png` posé directement sur le dégradé (pas de chip fixe nécessaire, le fond EST déjà la couleur de marque) + une inscription sobre "Ce que je peux faire maintenant…". Récap statistique ajouté sous le post-it (tâches totales assignées, projets totaux, actions au backlog) — un seul nouveau bout de donnée (`totalTaskCount`, capturé avant filtrage sur un fetch déjà existant), le reste dérivé de données déjà chargées. Repli mobile mis à jour en conséquence (logo en chip fixe, textes repassés en couleurs thémées). Voir `docs/charte-graphique.md` > "Écran d'accueil — panneau oblique v3" > "Retouches".

**Session du 2026-08-10 (suite quater) — terminée** : deux corrections + refonte de l'accueil. (1) Animation de la signature `LoginPage` corrigée : le portage `motion` déclaratif de la passe précédente ne rejouait pas comme prévu — remplacé par un portage fidèle de `watodo-logo-animation.html` (fournie en référence vérifiée fonctionnelle), transition CSS sur `stroke-dashoffset` pilotée via `useEffect`/refs plutôt que le prop `pathLength` de motion ; `prefers-reduced-motion` vérifié à la main (`matchMedia`) puisque `motion/react` n'est plus utilisé pour cette animation. (2) Écran d'accueil refondu (v3, panneau oblique), reprise de `maquette-accueil-v3-oblique.html` livrée comme base à peaufiner. Changement d'architecture : `HomePage` sort d'`AppShell` et se rend désormais en plein viewport comme `LoginPage` (la prop `hideNav` d'`AppShell`, devenue inutile, est supprimée). Quatre points explicitement demandés : accès rapide qui passe sous la barre oblique (correction de l'ordre d'empilement), post-it incorporé au fond plutôt que collé dessus (fond translucide teinté crème au lieu d'un aplat neutre), petit bouton de déconnexion en haut à droite (nouveau, gère connexion réelle et mode démo), salutation dactylographiée avec le prénom/identifiant réel de la personne connectée (plus de texte générique "utilisateur"). Contrairement à `LoginPage`, cet écran **suit le thème clair/sombre** (aucune consigne de figer les couleurs cette fois) — presque toutes les valeurs de la maquette recoupaient déjà les jetons du mode sombre. Voir `docs/charte-graphique.md` > "Écran d'accueil — panneau oblique v3" pour le détail complet (dont deux écarts pris d'initiative : couleurs par onglet abandonnées, positions calculées plutôt que codées en dur).

**Session du 2026-08-10 (suite ter) — terminée** : refonte de la page de connexion (`LoginPage`), reprise de `maquette-login-oblique.html` fournie par l'utilisateur. Consigne inhabituelle, appliquée à la lettre : polices adaptées à la charte (`var(--font-display)`/`var(--font-mono)` auto-hébergées, plus de lien Google Fonts), **couleurs non adaptées** — reprises telles quelles en variables locales à `.login-page` (`--login-accent`, `--login-cream`...), pas remappées sur `theme/tokens.css`. Conséquence assumée : cet écran reste figé en mode clair, ne suit pas le `ThemeToggle` (absent de cet écran, comme avant). Signature animée de la maquette (tracé + point qui rebondit, script JS impératif dans la source) portée en `motion` déclaratif (`pathLength`, `times`/`ease`) — **ne rejouait pas correctement en pratique, corrigé dans la session suivante** (voir entrée du dessus). Fonctionnalités du formulaire réel préservées (identifiant + mot de passe, pas email — auth du produit inchangée), gestion d'erreur, bouton Annuler conditionnel, et panneau "Mode démo" (absent de la maquette) réintégré sous la carte plutôt qu'à côté. Bouton "Continuer avec Microsoft" repris visuellement mais non fonctionnel (SSO différé) — clic → toast "Pas encore disponible.". Repli responsive sous 720px (empilement vertical, bandeau de marque masqué), absent de la maquette. Voir `docs/charte-graphique.md` > "Page de connexion — panneau oblique".

**Session du 2026-08-10 (suite) — terminée** : logo définitif + renommage produit. Fichier corrigé fourni directement par l'utilisateur (`watodo-logo-transparent-cream.png`, fond réellement transparent + marque recolorée en crème `--sidebar-text`, `#F5E7E3`, plutôt que le brique d'origine quasi invisible sur `--sidebar-bg`) — copié en `frontend/src/assets/awtodo-logo.png`, remplace le wordmark texte "Watodo" posé en fallback lors de la passe précédente. Placement clarifié par question explicite à l'utilisateur (la sidebar 210px visée par la demande n'existe plus depuis le 08/08) : `.topbar__brand` dans `Topbar`, seul endroit, pas de duplication. Le crème n'étant lisible que sur le rouge de marque fixe, `.topbar__brand` devient un chip à fond `--sidebar-bg` autour du logo plutôt qu'une pose directe sur le fond thémé de la topbar. **Au même moment, renommage du produit : Watodo → Awtodo** — appliqué aux titres/paragraphes définitionnels de ce fichier et des trois `docs/*.md`, aux textes UI (`LoginPage`, `InvitationAcceptPage`), aux emails d'invitation et leur sujet, au titre/description de l'API (`SPECTACULAR_SETTINGS`), à l'adresse d'expédition par défaut (`no-reply@watodo.local` → `no-reply@awtodo.local`). **Délibérément non touché** : les entrées datées de ce Roadmap (journal chronologique, "Watodo" y reste exact pour décrire l'état à l'époque), les artefacts de référence figés fournis par l'utilisateur (templates HTML, comptes-rendus), le nom du dépôt. `.env.example` non vérifié (accès refusé par les permissions de l'environnement) — à contrôler manuellement. Voir `docs/charte-graphique.md` > "Logo définitif + renommage Watodo → Awtodo" pour le détail complet.

**Session du 2026-08-10 — terminée** : intégration d'un logo et d'un template de header fournis par l'utilisateur (`awtodo_logo_handwritten_2.png`, `watodo-header-final.html`, copiés à la racine du repo, explicitement un template de référence à adapter, pas à copier brut). Logo vérifié via Pillow : fond opaque (canal alpha à 255 sur la totalité des pixels de l'image), pas de transparence exploitable — conformément à la consigne, pas de contournement (`mix-blend-mode`/recadrage), remplacé par un wordmark texte "Watodo" (`--font-display`, poids 800, `--color-accent`). Le template supposait une sidebar de navigation 210px qui n'existe plus depuis la session du 08/08/2026 (remplacée par `BinderTabs`+`SectionSidebar`, voir plus bas) — adapté plutôt que suivi littéralement, sans recréer de deuxième colonne : `Topbar` restructuré en trois zones (wordmark à gauche, recherche centrée au milieu — même déclencheur de palette de commandes `Command+K` existant, juste restylé —, actions à droite), nouveaux boutons Notifications/Paramètres (icônes `lucide-react`, hors périmètre v1 donc toast "Pas encore disponible" au clic plutôt qu'une fausse fonctionnalité), nouveau composant `Breadcrumb` sous la topbar qui reprend le rôle de l'ancien titre de page dynamique (qui affichait "Projets"/nom du projet/etc. dans `Topbar`, désormais fixe sur "Watodo"). Au passage, correction d'un jeton mal choisi (pas visuel) : l'avatar de `UserMenu` réutilisait `--task-status-inprogress-bg/text` au lieu de `--color-accent`/`--color-accent-contrast`. Voir `docs/charte-graphique.md` > "Header — logo et recherche centrée" pour le détail complet des écarts assumés face au template.

**Session du 2026-08-08 (suite ter) — terminée** : ajouts sur l'onglet Budgétisation, toujours par référence à des composants réels Watermelon UI. Nouveau `BudgetTrendWidget` (+ composant générique `AreaChart`, fait main) : évolution cumulative du montant engagé (OPEX+CAPEX), dans l'ordre chronologique réel des lignes — pas de découpage 7j/14j/30j comme le composant source (`widget-5`), qui n'aurait pas de sens sur des lignes budgétaires ponctuelles. Nouvelle répartition OPEX/CAPEX (`widget-4` adapté) : réutilise directement `DonutChart` déjà existant, pas de nouveau composant. **`recharts` (dépendance des deux composants source) délibérément pas ajouté** — Watodo a déjà ses propres graphiques faits main, doublon évité. Table des membres de projet (`ProjectAdminTab`, déjà une vraie table) restylée avec avatar-initiales + pastille de statut de compte (`table-15`/`dropdown-menu-7` combinés) — reste toujours visible, pas de dropdown caché derrière un bouton (demande explicite). Voir `docs/charte-graphique.md` > "Budgétisation — widgets d'ensemble + table des membres" et `docs/modeles-et-api.md` > "Widgets d'ensemble".

**Session du 2026-08-08 (suite bis) — terminée** : (1) fix du panneau de filtre qui décalait la page à l'ouverture (`StatusFilterDropdown` sans taille propre). (2) Écrans Tâches/Incidents : le clic sur une ligne déplie désormais un accordéon en place (`TaskAccordion`/`IncidentAccordion`, pattern Watermelon UI `expandable-event-card` adapté — animation de hauteur, pas de shared-layout, le placement "sous la ligne" ne s'y prêtant pas) au lieu d'ouvrir un drawer latéral — `TaskDrawer` original conservé tel quel pour le Kanban/Roadmap d'un projet (contextes en cartes, pas concernés). Nouveaux endpoints backend `update-description` (tâches et incidents, même garde que `rename`/`comments`) + nouveau composant `InlineEditableTextarea` pour l'éditer en un clic direct sur le texte (validation par boutons explicites, jamais au blur/Entrée). (3) Nouveau `DatePickerField` (pattern Watermelon UI `date-picker-4` adapté, nouvelle dépendance `react-day-picker`) remplace les `<input type="date">` sur les échéances (projet, tâche). (4) `NotesTab` passe en édition inline (même composant `InlineEditableTextarea`) ; nouvel export `.docx` du cahier des charges (`SpecTab`, nouvelle dépendance `docx`) — PDF délibérément écarté, l'objectif explicite étant un format réellement rééditable dans Word. Voir `docs/charte-graphique.md` > "Accordéon Tâches/Incidents + sélecteur de date".

**Session du 2026-08-08 (suite) — terminée** : trois patterns d'interaction adaptés de composants réels **Watermelon UI** (registre `registry.watermelon.sh`, code source récupéré via `curl` — `WebFetch` seul reçoit un 403 sur ce registre). (1) `StatusFilterDropdown` (filtres Tâches/Incidents/Projets, composant unique partagé) : bouton rond qui se déplie en panneau (transition d'élément partagé Motion), reste **multi-sélection** contrairement au composant source (sélection unique) — le comportement de filtrage ne change pas, seul l'habillage. (2) `ProjectsGrid` : le clic sur une card projet l'agrandit désormais sur place (au lieu de naviguer directement) vers une vue détaillée (badges/groupe assigné/description/progression) avec un bouton "Accéder au projet" qui, lui, navigue réellement — pas de photo (contrairement au composant source, les projets n'en ont pas). (3) `SectionSidebar` : highlight de survol qui glisse entre les items (transition d'élément partagée), pas de mode replié/icônes (les items n'en ont pas). Au passage, retour direct sur la passe précédente : `BinderTabs` reprend le rouge de marque fixe (`--sidebar-bg`, même couleur que `SectionSidebar`) au lieu de suivre le thème, et `Topbar` récupère la couleur que portait `BinderTabs` auparavant (`--color-surface`). Voir `docs/charte-graphique.md` > "Patterns Watermelon UI adaptés" et "Onglets classeur".
**Session du 2026-08-08 (suite) — terminée** : trois patterns d'interaction adaptés de composants réels **Watermelon UI** (registre `registry.watermelon.sh`, code source récupéré via `curl` — `WebFetch` seul reçoit un 403 sur ce registre). (1) `StatusFilterDropdown` (filtres Tâches/Incidents/Projets, composant unique partagé) : bouton rond qui se déplie en panneau (transition d'élément partagé Motion), reste **multi-sélection** contrairement au composant source (sélection unique) — le comportement de filtrage ne change pas, seul l'habillage. (2) `ProjectsGrid` : le clic sur une card projet l'agrandit désormais sur place (au lieu de naviguer directement) vers une vue détaillée (badges/groupe assigné/description/progression) avec un bouton "Accéder au projet" qui, lui, navigue réellement — pas de photo (contrairement au composant source, les projets n'en ont pas). (3) `SectionSidebar` : highlight de survol qui glisse entre les items (transition d'élément partagée), pas de mode replié/icônes (les items n'en ont pas). Au passage, retour direct sur la passe précédente : `BinderTabs` reprend le rouge de marque fixe (`--sidebar-bg`, même couleur que `SectionSidebar`) au lieu de suivre le thème, et `Topbar` récupère la couleur que portait `BinderTabs` auparavant (`--color-surface`). Voir `docs/charte-graphique.md` > "Patterns Watermelon UI adaptés" et "Onglets classeur".

**Session du 2026-08-08 — terminée** : abandon de la sidebar globale (`Sidebar.tsx`/`.css`, supprimés). Remplacée par deux niveaux de navigation distincts : (1) premier niveau (Projets/Tâches/Incidents/Statistiques/Administration) en **onglets "classeur"** horizontaux en haut de l'écran (`layout/BinderTabs.tsx`, entre `Topbar` et le contenu, suit le thème clair/sombre) ; (2) sous-menus déjà existants à l'intérieur d'une section (tabs du hub projet, tabs d'Administration) convertis en **sidebar locale** (`components/SectionSidebar.tsx`, composant partagé) qui reprend le traitement couleur fixe de l'ancienne sidebar globale (`--sidebar-*`, indépendant du thème). `AppShell` restructuré en conséquence (`hideSidebar` renommé `hideNav`, toujours réglé uniquement sur la route d'accueil). Le titre du `Topbar` redevient cliquable vers l'accueil (rôle auparavant tenu par le bouton "Watodo" de la sidebar, disparu avec elle). Voir `docs/charte-graphique.md` > "Architecture de page — navigation" et "Patterns UI actés" > "Onglets classeur".

**Session du 2026-08-07 (suite ter) — terminée** : refonte complète de la palette de couleurs (v4, abandon de l'ancienne inversion cyan/violet-galaxie au profit d'une couleur de marque brique constante dans les deux modes, `#753030` clair/`#8F4040` sombre) + écran d'accueil sans sidebar (tous les menus de navigation, y compris Statistiques et Administration, désormais proposés sous forme de cards sur `HomePage` — `AppShell` gagne une prop `hideSidebar`, activée uniquement sur la route `home`). Typographie/formes/badges/animations inchangés, seules les valeurs de couleur ont bougé — voir `docs/charte-graphique.md` pour le détail complet (valeurs hex finales, contrastes WCAG réellement calculés, précisions d'implémentation).

**Session du 2026-08-07 (suite) — terminée** : incidents sans projet, rattachés à un groupe (l'outil de ticketing externe, toujours pas branché, pourra créer un incident sans connaître de projet Watodo précis — le groupe concerné devient alors obligatoire côté outil externe, pas dans le modèle Watodo). `Incident.project` devient nullable, nouveau `Incident.team` (nullable) — exactement l'un des deux, jamais les deux, jamais aucun (validation service-layer dans `create_incident`, pas de contrainte DB). Nouvelle **boîte de réception** (`GET /api/v1/incidents/inbox/`) : incidents non-affectés visibles seulement par les membres du groupe concerné, avec **réassignement vers un projet** (`POST /api/v1/incidents/{id}/assign-project/`, nouveau flag `can_assign_project`) — un incident quitte alors définitivement la boîte de réception, pas de chemin retour. Généralisation au passage des fonctions de garde de `apps/incidents/services.py` (`_require_member` prend désormais l'incident, pas son projet) — résout la tension Team/ProjectMembership documentée depuis la passe scoping, au moins pour les incidents non-affectés. Formulaire de création manuelle resté project-only (confirmé, pas de choix de groupe ajouté côté UI). Au passage : onglet hub projet "Maintenance" relabellisé "Incidents" (fonctionnalité déjà en place), bande d'accueil "À traiter" renommée "Backlog" et incidents affichés désormais filtrés sur les priorités haute/critique (comme les tâches, qui l'étaient déjà). Voir `docs/modeles-et-api.md` > "Incident" > "Boîte de réception des incidents non-affectés" et "API — endpoints Incidents".

**Session du 2026-08-07 — terminée** : trois points d'évolution suite aux tests de la passe auth. (1) La connexion devient la porte d'entrée obligatoire du site (`App.tsx` bloque tout accès tant qu'aucun `currentUser` n'existe) — le sélecteur "mode démo" reste utilisable, dupliqué sur `LoginPage.tsx` pour rester atteignable dès ce premier écran (ex. se connecter directement en tant qu'`is_platform_admin=True` pour tester avec tous les droits). (2) Corollaire direct du point précédent, pas de changement séparé. (3) Administration des groupes complétée : renommage (`PATCH /api/v1/accounts/teams/{id}/rename/`, titre éditable au clic dans `GroupsSection.tsx`) + élargissement de "administrateur du groupe" (créateur **ou** admin d'organisation/de plateforme, pas seulement le créateur comme avant) sur l'ajout/retrait de membres et le renommage. Voir `docs/organisation-et-comptes.md` > "Administration des groupes" et `docs/modeles-et-api.md` > "Mécanisme d'identification temporaire".

**Session du 2026-08-06 (soir) — terminée** : authentification par mot de passe, indépendante du SSO (toujours différé) — remontée client explicite après la passe Mailtrap. "S'inscrire sans SSO" = compléter le flux d'invitation existant en choisissant un mot de passe à l'activation (pas d'inscription publique ouverte). Nouveaux `POST /api/v1/accounts/login/` et `GET /api/v1/accounts/me/` (JWT via `djangorestframework-simplejwt`, enfin câblé), nouvelle page `LoginPage.tsx` atteignable depuis `UserMenu`. Le sélecteur de test `X-Debug-User-Id` n'est pas retiré, les deux mécanismes coexistent (un seul actif à la fois côté frontend). Voir `docs/organisation-et-comptes.md` > "Authentification par mot de passe" et la section "Stack technique" > Auth ci-dessus.

**Session du 2026-08-06 (suite) — terminée** : correctif UX — à la création d'un projet collaboratif, choisir un groupe coche désormais par défaut tous ses membres actifs (décochable au cas par cas), au lieu de partir d'une sélection vide à cocher un par un. Purement frontend (`ProjectCreateDialog.tsx`), le backend gérait déjà correctement `member_ids` quel que soit son contenu. Voir `docs/organisation-et-comptes.md` > "Sélection de membres à la création d'un projet collaboratif" > correction du 2026-08-06.

**Session du 2026-08-06 — terminée** : correctif de permission — un chef de projet peut désormais clôturer une tâche même si elle n'est pas assignée à lui-même (`_ensure_can_complete`, auparavant réservé au seul assigné). La réattribution d'une tâche déjà assignée par un chef de projet fonctionnait déjà correctement côté backend (vérifié) mais n'avait **aucune interface** côté frontend — ajout d'une section "Attribution" dans `TaskDrawer` (sélecteur de membre + bouton Assigner/Réattribuer). Au passage, correction d'un bug connexe : le champ "Assigné à" du formulaire de création de tâche affichait tous les utilisateurs de la plateforme au lieu des seuls membres du projet. Voir `docs/modeles-et-api.md` > "API — endpoints de transition de statut (tâches)" > les trois corrections du 2026-08-06.

**Session du 2026-08-05, nuit — terminée** : cahier des charges restructuré en 12 sous-sections fixes, non obligatoires (Contexte/Objectifs/Besoin/Périmètre/Exigences fonctionnelles/Exigences techniques/Contraintes/Livrables/Planning/Budget/Organisation/Annexes) — sommaire à cocher à gauche, document composé des sections cochées à droite. Remplace l'ancien champ unique `spec_content` (retiré du modèle `Project`, nouveau modèle `SpecSection`) — voir `docs/modeles-et-api.md` > "Projets — Hub complet" > "1. Cahier des charges".

**Session du 2026-08-05, soir ter — terminée** : encore un retour client sur les Statistiques ("ça manque toujours de GRAPHIQUES"), après une référence externe (`ui.watermelon.sh`) inaccessible dans cet environnement (SPA 100% JS, fetcher bloqué) — l'utilisateur a fourni une capture d'écran d'un dashboard générique à la place. Ajout d'un graphique en barres "tâches terminées par semaine" (8 semaines glissantes), refonte des cards KPI en gabarit "chiffre héros" (libellé + icône en haut, grande valeur en dessous), donut de respect des échéances agrandi — voir `docs/modeles-et-api.md` > "Statistiques" > "Graphique en barres + refonte visuelle des cards". Toujours traduit dans la palette Watodo existante, aucune nouvelle couleur/dégradé.

**Session du 2026-08-05, soir bis — terminée** : retour client sur la passe précédente — (1) statistiques jugées trop pauvres en graphiques/widgets pour un usage "gestionnaire de projets" : ajout de widgets délais/heures passées/respect des échéances/personnes sollicitées sur l'onglet projet **et** l'écran global (voir `docs/modeles-et-api.md` > "Statistiques" > "Widgets étendus"), (2) quantité des lignes de budget corrigée en unités entières, plus de décimales (voir `docs/modeles-et-api.md` > "Budgétisation" > "Correction — quantité en unités entières").

**Session du 2026-08-05, soir — terminée** : onglet Statistiques par projet (cards + tableau par membre), écran Statistiques global (sidebar, KPI + budgets des projets dirigés), onglet Budgétisation par projet (OPEX/CAPEX, tableaux éditables + camemberts) — voir `docs/modeles-et-api.md` > "Statistiques" et "Budgétisation". Point resté ouvert : le modèle `BudgetEntry` (temps passé × coût horaire) n'est toujours pas branché fonctionnellement, voir "Hors périmètre v1".

**Session du 2026-08-05 (après-midi) — terminée** : montée en qualité visuelle générale (relief/formes/dynamisme, voir "Patterns UI actés" > "Montée en qualité visuelle"), Roadmap en carte dédiée (grillage déjà présent avant cette session), scoping des listes par appartenance, conditionnement des actions aux droits (flags `permissions`). Point resté ouvert : tension notée entre scoping (`ProjectMembership`) et autorisation d'action sur les incidents (`Team`) — voir `docs/modeles-et-api.md` > "Scoping des listes par appartenance" > précisions, pas urgent mais à garder en tête.

Après ces passes : reste de la finition esthétique au fil de l'usage. Ordre indicatif, pas gravé.

**Validation reportée au déploiement (liste consolidée) :** trois chantiers sont construits et testables en logique dès maintenant, mais leur vérification bout-en-bout attend le déploiement AWS — ne pas chercher à les simuler intégralement en local avant ça :
- **Invitations par email** — *nuancé, session du 2026-08-06 : le contenu/pattern des emails (objet, corps, lien) est désormais testable en local avec de vrais emails via une boîte sandbox (Mailtrap), en configurant `.env` (voir `.env.example` et `docs/organisation-et-comptes.md` > "Comptes et invitations"). Reste reporté au déploiement : la déliverabilité réelle (fournisseur transactionnel type Mailgun, domaine d'expédition, DKIM/SPF).*
- **SSO Microsoft** (tenant Azure AD réel requis)
- **Webhook ticketing entrant** (connexion au vrai outil de ticketing requise)

## Comment utiliser ce fichier

- Ce fichier est la source de vérité sur les **décisions déjà tranchées**. Ne pas les remettre en question sans validation explicite.
- Pour toute question non couverte ici (nouvelle dépendance, pattern non défini, arbitrage entre deux approches, et tous les points listés en "hors périmètre v1"), **demander confirmation avant d'implémenter** plutôt que de trancher seul.
- Les décisions concernant le schéma de BDD détaillé et la config infra AWS précise seront ajoutées à ce fichier au fur et à mesure — elles ne sont pas encore figées à ce stade. Voir l'encadré d'avertissement dans la section "Stack technique" : ces deux sujets sont explicitement bloqués pour l'implémentation tant que ce fichier n'a pas été mis à jour en conséquence.
